This chunk is from the HTML file: OLF_AVS_Script_Bulk_Import.htm.
 Section heading: Fields.
 Text content: This window contains 
	 the following fields: .
 Image file: OLF_AVS_Script_Bulk_Import_files/image007.jpg
 Image description: a black and white map of the united states.
 Table data: 
| Field | Description |
| --- | --- |
| Select | Indicates 
			 that plugin/script listed should be processed. |
| File Name | File name and path of the .java file 
			 to be imported. |
| Plugin/Script Name | Plugin/Script Name in the OLF system. |
| Plugin/Script Type | Indicates the type of plugin/script. 
			 There are five different types:TypeDescriptionParamAParameterplugin/script is used to prepare information to be 
					 passed to a main plugin/script. The tableargtis used to hold data to be passed to a main plugin/script. 
					 Parameter plugin/scripts can, but are not normally, 
					 run without a main plugin/script in a Task.MainAMainplugin/script is the main plugin/script that contains 
					 the bulk of the reporting or process-specific code. 
					 A main plugin/script may be designated to run alone 
					 in a Task.Page 
					 ViewerAPage 
					 Viewerplugin/script is used to set the subscriptions 
					 and activate the Page Viewer. This kind of plugin/script 
					 may run alone in a Task.OutputAnOutput 
					 Plugin/scriptis a plugin/script that runs 
					 locally in the Trading Manager after the main plugin/script 
					 has run.Warning: Output Plugin/scripts 
					 cannot be run alone in a Task. They require a Main 
					 plugin/script in the Task as well.IncludeThis means the 
					 plugin/script can be used as an include plugin/script. 
					 This is so functions in one plugin/script can be shared.This required a license key (AVS_POUND_INCLUDE) 
					 in builds before August 27, 2003. It now is not controlled 
					 by licensing. | Type | Description | Param | AParameterplugin/script is used to prepare information to be 
					 passed to a main plugin/script. The tableargtis used to hold data to be passed to a main plugin/script. 
					 Parameter plugin/scripts can, but are not normally, 
					 run without a main plugin/script in a Task. | Main | AMainplugin/script is the main plugin/script that contains 
					 the bulk of the reporting or process-specific code. 
					 A main plugin/script may be designated to run alone 
					 in a Task. | Page 
					 Viewer | APage 
					 Viewerplugin/script is used to set the subscriptions 
					 and activate the Page Viewer. This kind of plugin/script 
					 may run alone in a Task. | Output | AnOutput 
					 Plugin/scriptis a plugin/script that runs 
					 locally in the Trading Manager after the main plugin/script 
					 has run.Warning: Output Plugin/scripts 
					 cannot be run alone in a Task. They require a Main 
					 plugin/script in the Task as well. | Include | This means the 
					 plugin/script can be used as an include plugin/script. 
					 This is so functions in one plugin/script can be shared.This required a license key (AVS_POUND_INCLUDE) 
					 in builds before August 27, 2003. It now is not controlled 
					 by licensing. |
| Type | Description |
| Param | AParameterplugin/script is used to prepare information to be 
					 passed to a main plugin/script. The tableargtis used to hold data to be passed to a main plugin/script. 
					 Parameter plugin/scripts can, but are not normally, 
					 run without a main plugin/script in a Task. |
| Main | AMainplugin/script is the main plugin/script that contains 
					 the bulk of the reporting or process-specific code. 
					 A main plugin/script may be designated to run alone 
					 in a Task. |
| Page 
					 Viewer | APage 
					 Viewerplugin/script is used to set the subscriptions 
					 and activate the Page Viewer. This kind of plugin/script 
					 may run alone in a Task. |
| Output | AnOutput 
					 Plugin/scriptis a plugin/script that runs 
					 locally in the Trading Manager after the main plugin/script 
					 has run.Warning: Output Plugin/scripts 
					 cannot be run alone in a Task. They require a Main 
					 plugin/script in the Task as well. |
| Include | This means the 
					 plugin/script can be used as an include plugin/script. 
					 This is so functions in one plugin/script can be shared.This required a license key (AVS_POUND_INCLUDE) 
					 in builds before August 27, 2003. It now is not controlled 
					 by licensing. |
| Already Exist | Yes indicates the plugin/scripts has 
			 been previously imported. |
| Overwrite? | Yes indicates that the existing plugin/scripts 
			 should be overwritten. |
| Save On Comp Error | This appears before the plugin/scripts 
			 have been imported. The default of No is the only option as 
			 a plugin/script with compile errors will not be saved. |
| Properties | Displays parameters associated with 
			 the plugin/script (i.e. UserParm – Read+Write+Execute). |
| Categories | Category of the plugin/script. Right 
			 click for a pick list of Categories. |
| Saved | This appears only if the plugin/script 
			 has been saved. |
| Reason | Displays OK if the plugin/script was 
			 imported successfully or displays a reason why it was not 
			 imported. |.