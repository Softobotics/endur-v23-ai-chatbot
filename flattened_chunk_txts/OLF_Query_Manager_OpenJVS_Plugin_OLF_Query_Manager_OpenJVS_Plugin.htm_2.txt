This chunk is from the HTML file: OLF_Query_Manager_OpenJVS_Plugin.htm.
 Section heading: Example.
 Text content: For example, if the Query Manager is called 
 from the Trading Manager main window, then the Trading Manager browser 
 will show the effect of the script.  The scripts should usually 
 return with an EXIT_Succeed.   EXIT_Fail will cause the 
 query engine to not execute subsequent query pages, but pages already 
 processed (including the page that the script is referenced from) will 
 be saved in the query_result table.  A good use for this 
 script is to pull in the hedge deals for each deal selected in the original 
 query.  A menu-driven text field has been added to the query 
 window, to allow the user to select a script.  When the user 
 saves the query, the selected scripts are stored as part of the query, 
 allowing the same scripts to be loaded back in when the saved query is 
 reloaded. It should be noted here that when a user runs a query 
 with one or more associated scripts, the results are returned in a table 
 called “argt.”  The script passes the following significant columns 
 in the argt table:  Other columns are passed in argt, but not set are 
 as above:.
 Table data: 
| Column | Type | Description |
| --- | --- | --- |
| "QueryId" | COL_INT | Unique 
		 query_id for the query_result created |
| "PageNum" | COL_INT | Query 
		 window page number currently being processed |
| "DoStatus" | COL_INT | Set 
		 to one of the following values:ValueTypeDescription0DO_CLEANSelect from scratch1DO_APPENDAdd to existing query_result2DO_DELETERemove from existing query_result | Value | Type | Description | 0 | DO_CLEAN | Select from scratch | 1 | DO_APPEND | Add to existing query_result | 2 | DO_DELETE | Remove from existing query_result |
| Value | Type | Description |
| 0 | DO_CLEAN | Select from scratch |
| 1 | DO_APPEND | Add to existing query_result |
| 2 | DO_DELETE | Remove from existing query_result |. Table data: 
| Column | Type |
| --- | --- |
| "Currency" | COL_INT |
| "Portfolio" | COL_INT |
| "SimRunId" | COL_INT |
| "SimDefId" | COL_INT |
| "RunType" | COL_INT |
| "ClientPid" | COL_INT |
| "QueryName" | COL_STRING |
| "RefreshMktd", | COL_INT |
| "UseClose" | COL_INT |
| "ClearCache" | COL_INT |
| "MarketData" | COL_TABLE |
| "SimulationDef" | COL_TABLE |
| "UseMarketPrices" | COL_INT |
| "AsmId" | COL_INT |.